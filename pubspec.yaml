name: my_event_planner
description: A new Flutter project for event planning.

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  video_player: ^2.10.0
  intl: ^0.18.1  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sample_event.jpg
    - assets/sample_video.mp4
